#include <bits/stdc++.h>
using namespace std;
int main()
{
    //its map without values from key values pair.   
    //set <string> s; // here all the elements are stored in ascending sorted order.
    //s.insert("4"); // insertion takes log(n)
    //s.find("4") // finding takes log(n) , it returns a iterator to 4.
    //s.erase("4"); // takes log(n)
    // ordered set is also implemented via red black trees.
    //UNORDERED SET
    // its implemented thru hash.
    //now insertion finding erasing takes O(1).
}